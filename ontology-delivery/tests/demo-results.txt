演示对象：本地 Mock API；即将重置 ws-demo。服务需 ENABLE_DEMO_RESET=true。
1. 当前正式版本 v1.3.0；类型定义 31 个。
2. r12 实际校验发现：runtime 依赖未锁定版本。
3. 修复后生成 r13，正式 v1.3.0 内容未改变。
4. r13 校验通过；影响清单范围 PARTIAL，不声称全企业覆盖。
5. 已发布 v1.4.0；未启动工作流、未修改消费方固定版本。
6. 固定用例：低分但有依据可确认；高分仅 LLM 依据被拒绝。均未执行真实动作。
DEMO_PASS：HTTP 模型管理链路可重复；九个 React 页面需按交付规范接入。
